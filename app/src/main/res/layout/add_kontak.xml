<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ImageButton for adding -->
    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/addButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/add"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- TextView for "Nama" -->
    <TextView
        android:id="@+id/namaLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nama"
        app:layout_constraintStart_toEndOf="@id/addButton"
        app:layout_constraintTop_toTopOf="@id/addButton"
        android:layout_marginStart="16dp"/>

    <!-- EditText for name -->
    <EditText
        android:id="@+id/nm"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/namaLabel"
        app:layout_constraintTop_toBottomOf="@id/namaLabel"
        android:layout_marginTop="8dp"/>

    <!-- TextView for "No Hp" -->
    <TextView
        android:id="@+id/noHpLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No Hp"
        app:layout_constraintStart_toEndOf="@id/addButton"
        app:layout_constraintTop_toBottomOf="@id/nm"
        android:layout_marginTop="16dp"/>

    <!-- EditText for phone number -->
    <EditText
        android:id="@+id/hp"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/noHpLabel"
        app:layout_constraintTop_toBottomOf="@id/noHpLabel"
        android:layout_marginTop="8dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
